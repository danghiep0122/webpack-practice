import React from "react";
import { hot } from "react-hot-loader/root";

function App() {
    return (
        <main>
            <h1>Hello world</h1>
        </main>
    );
}

export default hot(App);
